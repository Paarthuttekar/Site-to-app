import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ga.paarth.LazycarrotStudios',
  appName: 'Lazycarrot Studios',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
